# Modelo de configuração de ambiente.
# Copie este arquivo para um arquivo chamado ".env" e ajuste os valores.
# O arquivo .env NÃO é versionado (está no .gitignore) — não comite credenciais.

DB_HOST=localhost
DB_PORT=3306
DB_NAME=sistema_automotivo
DB_USERNAME=root
DB_PASSWORD=sua_senha_aqui
